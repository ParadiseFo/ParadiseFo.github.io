# 复制为 mysql.env（勿提交 mysql.env）后按需修改：
#   copy docker\mysql.env.example docker\mysql.env   (PowerShell 在仓库根目录)
#
MYSQL_ROOT_PASSWORD=change-me-to-a-strong-password
# docker-compose 将容器 3306 映射到本机 3307；backend/.env 的 DATABASE_URL 端口请用 3307
