# 后端 API 根地址（无尾斜杠）。
#
# 【模式 B】前台与管理员共用数据库文章时：
# - 本地：指向本机 API（与 backend 同时运行）
PUBLIC_API_BASE=http://127.0.0.1:4000
#
# - GitHub Actions：在仓库 Settings → Secrets → Actions 中设置同名变量，必须为公网 HTTPS，例如：
# PUBLIC_API_BASE=https://xxx.up.railway.app
#
# 若不设置 PUBLIC_API_BASE，构建将使用 src/content/blog 的 Markdown（与管理员无关）。
